apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: e-shop
spec:
  accessModes:
    - ReadWriteOnce   # one Pod can read/write at a time
  resources:
    requests:
      storage: 1Gi
