apiVersion: v1
kind: Service
metadata:
  name: redis           # basket-service reaches Redis at "redis:6379"
  namespace: e-shop
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
